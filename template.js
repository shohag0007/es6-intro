const a = 10;
const b = 20;
const numbers = [45, 48, 98, 78];
const student = {name:'shohag', age: 35};
const math = `The sum of ${a} and ${b} is ${a+b}`;
console.log(math);

const challenge = `hi everyone, how are you 
everybody.
we are waiting for you`;
console.log(challenge);
const result =`the sum of ${numbers[0]} and ${student.age} is ${numbers [0] + student.age}`
console.log(result);